export function estadoMateria(estado: 'solido' | 'liquido' | 'gasoso'): void{
    console.log(`O estado da matéroa escolhido é ${estado}`);
}

export function imprimeDoc(doc: number | string): void{
    console.log(`A numeração do seu documento é ${doc}`);
}